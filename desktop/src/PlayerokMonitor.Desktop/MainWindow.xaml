<Window x:Class="PlayerokMonitor.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:local="clr-namespace:PlayerokMonitor.Desktop"
        Title="Playerok Monitor" Width="1280" Height="820" MinWidth="980" MinHeight="650"
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="CanResizeWithGrip"
        Background="{StaticResource PageBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="44" ResizeBorderThickness="7" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="44"/><RowDefinition Height="*"/></Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#0D1016" BorderBrush="#20FFFFFF" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="46"/><ColumnDefinition Width="46"/><ColumnDefinition Width="52"/></Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <Border Width="24" Height="24" CornerRadius="8" Background="{StaticResource AccentBrush}">
                        <TextBlock Text="P" FontWeight="Bold" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Playerok Monitor" Margin="10,0,0,0" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Grid.Column="1" x:Name="MinimizeButton" Content="—" FontSize="15" Padding="0" BorderThickness="0" shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="MinimizeButton_Click"/>
                <Button Grid.Column="2" x:Name="MaximizeButton" Content="□" FontSize="15" Padding="0" BorderThickness="0" shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="MaximizeButton_Click"/>
                <Button Grid.Column="3" Content="×" FontSize="20" Padding="0" BorderThickness="0" shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="CloseButton_Click"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition Width="226"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border Background="#0D1016" BorderBrush="#20FFFFFF" BorderThickness="0,0,1,0">
                <Grid Margin="16,22,16,16">
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                    <StackPanel>
                        <TextBlock Text="PLAYEROK" Foreground="{StaticResource MutedBrush}" FontWeight="Bold" FontSize="11"/>
                        <TextBlock Text="Заказы" FontWeight="Bold" FontSize="27" Margin="0,2,0,0"/>
                    </StackPanel>
                    <Border Grid.Row="1" x:Name="ConnectionPill" Background="#1836D57B" CornerRadius="14" Padding="10,7" Margin="0,20,0,18">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse x:Name="ConnectionDot" Width="8" Height="8" Fill="{StaticResource GreenBrush}" Margin="0,0,8,0"/>
                            <TextBlock x:Name="ConnectionText" Text="Подключение…" FontSize="12" Foreground="{StaticResource MutedBrush}" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </Border>
                    <StackPanel Grid.Row="2">
                        <Button x:Name="OrdersNav" Style="{StaticResource NavButton}" Click="OrdersNav_Click"><StackPanel Orientation="Horizontal"><TextBlock Text="▦" FontSize="17" Width="30"/><TextBlock Text="Заказы"/></StackPanel></Button>
                        <Button x:Name="StatsNav" Style="{StaticResource NavButton}" Click="StatsNav_Click"><StackPanel Orientation="Horizontal"><TextBlock Text="⌁" FontSize="18" Width="30"/><TextBlock Text="Статистика"/></StackPanel></Button>
                        <Button x:Name="SettingsNav" Style="{StaticResource NavButton}" Click="SettingsNav_Click"><StackPanel Orientation="Horizontal"><TextBlock Text="⚙" FontSize="17" Width="30"/><TextBlock Text="Настройки"/></StackPanel></Button>
                    </StackPanel>
                    <StackPanel Grid.Row="3">
                        <TextBlock Text="DESKTOP · WINDOWS 11" FontSize="10" FontWeight="Bold" Foreground="#738096"/>
                        <TextBlock Text="Версия 1.0.0" FontSize="11" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Column="1" Margin="28,20,28,24">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock x:Name="PageTitle" Text="Заказы" FontSize="30" FontWeight="Bold"/>
                        <TextBlock x:Name="PageSubtitle" Text="Новые заказы, продажи и покупки" Foreground="{StaticResource MutedBrush}" FontSize="13" Margin="0,3,0,0"/>
                    </StackPanel>
                    <Border Grid.Column="1" x:Name="UpdateBanner" Visibility="Collapsed" Background="#263E91FF" BorderBrush="#554D9AFF" BorderThickness="1" CornerRadius="15" Padding="12,8" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal"><TextBlock x:Name="UpdateText" Text="Доступно обновление" VerticalAlignment="Center"/><Button x:Name="InstallUpdateButton" Style="{StaticResource AccentButton}" Content="Обновить" Padding="12,6" Margin="10,0,0,0" Click="InstallUpdateButton_Click"/></StackPanel>
                    </Border>
                    <Button Grid.Column="2" x:Name="RefreshButton" Content="↻" FontSize="23" Width="46" Height="46" Padding="0" Click="RefreshButton_Click" ToolTip="Обновить список"/>
                </Grid>

                <Grid Grid.Row="1" Margin="0,20,0,0">
                    <Grid x:Name="OrdersView">
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="260"/></Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="NewTab" Style="{StaticResource TabButton}" Content="Новые" Click="NewTab_Click"/>
                                <Button x:Name="SalesTab" Style="{StaticResource TabButton}" Content="Продажи" Click="SalesTab_Click"/>
                                <Button x:Name="PurchasesTab" Style="{StaticResource TabButton}" Content="Покупки" Click="PurchasesTab_Click"/>
                                <Button x:Name="AllTab" Style="{StaticResource TabButton}" Content="Все" Click="AllTab_Click"/>
                            </StackPanel>
                            <Grid Grid.Column="2">
                                <TextBox x:Name="SearchBox" TextChanged="SearchBox_TextChanged" Padding="12,8" ToolTip="Поиск по товару или пользователю"/>
                                <TextBlock x:Name="SearchHint" Text="Поиск по заказам" Foreground="#788493" FontSize="13" Margin="14,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False"/>
                            </Grid>
                        </Grid>
                        <Grid Grid.Row="1" Margin="0,14,0,0">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="44*" MinWidth="360"/><ColumnDefinition Width="14"/><ColumnDefinition Width="56*" MinWidth="430"/></Grid.ColumnDefinitions>
                            <Border Style="{StaticResource Card}" Padding="7">
                                <Grid>
                                    <ListBox x:Name="OrdersList" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" SelectionChanged="OrdersList_SelectionChanged">
                                        <ListBox.Resources>
                                            <Style TargetType="ListBoxItem">
                                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                                <Setter Property="Padding" Value="0"/>
                                                <Setter Property="Margin" Value="0,0,0,6"/>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderThickness" Value="0"/>
                                                <Setter Property="Template">
                                                    <Setter.Value><ControlTemplate TargetType="ListBoxItem"><Border x:Name="ItemRoot" Background="{TemplateBinding Background}" CornerRadius="16" Padding="14"><ContentPresenter/></Border><ControlTemplate.Triggers><Trigger Property="IsSelected" Value="True"><Setter TargetName="ItemRoot" Property="Background" Value="{StaticResource AccentSoftBrush}"/></Trigger><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ItemRoot" Property="Background" Value="#16FFFFFF"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ListBox.Resources>
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Grid>
                                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding DisplayName}" FontSize="15" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                                            <TextBlock Text="{Binding DirectionLabel}" Foreground="{StaticResource AccentBrush}" FontSize="12" FontWeight="SemiBold"/>
                                                            <TextBlock Text="  ·  " Foreground="{StaticResource MutedBrush}"/>
                                                            <TextBlock Text="{Binding PaidAtDisplay}" Foreground="{StaticResource MutedBrush}" FontSize="12"/>
                                                        </StackPanel>
                                                        <TextBlock Text="{Binding StateLabel}" Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="0,4,0,0"/>
                                                    </StackPanel>
                                                    <StackPanel Grid.Column="1" HorizontalAlignment="Right" Margin="12,0,0,0">
                                                        <TextBlock Text="{Binding PriceDisplay}" FontSize="14" FontWeight="Bold" HorizontalAlignment="Right"/>
                                                        <TextBlock Text="{Binding CounterpartyDisplay}" Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="0,7,0,0" HorizontalAlignment="Right"/>
                                                    </StackPanel>
                                                </Grid>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                    <StackPanel x:Name="EmptyOrders" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
                                        <Border Width="52" Height="52" CornerRadius="18" Background="#14FFFFFF"><TextBlock Text="✓" FontSize="26" Foreground="{StaticResource GreenBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                        <TextBlock x:Name="EmptyOrdersText" Text="Новых заказов нет" FontSize="17" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,13,0,0"/>
                                        <TextBlock Text="Всё выполнено" Foreground="{StaticResource MutedBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <Border Grid.Column="2" Style="{StaticResource Card}">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <StackPanel>
                                        <StackPanel x:Name="NoSelection" HorizontalAlignment="Center" Margin="0,110,0,0">
                                            <TextBlock Text="Выберите заказ" FontSize="19" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                            <TextBlock Text="Здесь появятся детали и доступные действия" Foreground="{StaticResource MutedBrush}" Margin="0,6,0,0"/>
                                        </StackPanel>
                                        <StackPanel x:Name="OrderDetails" Visibility="Collapsed">
                                            <TextBlock x:Name="DetailDirection" Foreground="{StaticResource AccentBrush}" FontSize="12" FontWeight="Bold"/>
                                            <TextBlock x:Name="DetailTitle" FontSize="24" FontWeight="Bold" TextWrapping="Wrap" Margin="0,5,0,0"/>
                                            <TextBlock x:Name="DetailPrice" FontSize="18" FontWeight="SemiBold" Margin="0,9,0,0"/>
                                            <WrapPanel Margin="0,16,0,0">
                                                <Button x:Name="OpenDealButton" Content="Открыть Playerok" Margin="0,0,8,8" Click="OpenDealButton_Click"/>
                                                <Button x:Name="WakeButton" Style="{StaticResource AccentButton}" Content="Я проснулся" Margin="0,0,8,8" Click="WakeButton_Click" Visibility="Collapsed"/>
                                                <Button x:Name="RelistButton" Style="{StaticResource AccentButton}" Content="Выставить снова" Margin="0,0,8,8" Click="RelistButton_Click" Visibility="Collapsed"/>
                                            </WrapPanel>
                                            <StackPanel x:Name="DetailFields" Margin="0,6,0,0"/>
                                        </StackPanel>
                                    </StackPanel>
                                </ScrollViewer>
                            </Border>
                        </Grid>
                    </Grid>

                    <ScrollViewer x:Name="StatsView" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <UniformGrid Columns="4">
                                <Border Style="{StaticResource Card}" Margin="0,0,10,0"><StackPanel><TextBlock Text="Чистыми" Foreground="{StaticResource MutedBrush}"/><TextBlock x:Name="StatNet" Text="0 ₽" FontSize="26" FontWeight="Bold" Margin="0,7,0,0"/></StackPanel></Border>
                                <Border Style="{StaticResource Card}" Margin="0,0,10,0"><StackPanel><TextBlock Text="Продажи" Foreground="{StaticResource MutedBrush}"/><TextBlock x:Name="StatSales" Text="0" FontSize="26" FontWeight="Bold" Margin="0,7,0,0"/></StackPanel></Border>
                                <Border Style="{StaticResource Card}" Margin="0,0,10,0"><StackPanel><TextBlock Text="Средний доход" Foreground="{StaticResource MutedBrush}"/><TextBlock x:Name="StatAverage" Text="0 ₽" FontSize="26" FontWeight="Bold" Margin="0,7,0,0"/></StackPanel></Border>
                                <Border Style="{StaticResource Card}"><StackPanel><TextBlock Text="Завершение" Foreground="{StaticResource MutedBrush}"/><TextBlock x:Name="StatCompletion" Text="0%" FontSize="26" FontWeight="Bold" Margin="0,7,0,0"/></StackPanel></Border>
                            </UniformGrid>
                            <Grid Margin="0,14,0,0">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="14"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <Border Style="{StaticResource Card}">
                                    <StackPanel><TextBlock Text="Доход за 14 дней" FontSize="18" FontWeight="SemiBold"/><TextBlock Text="Рассчитывается локально из загруженных заказов" Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="0,3,0,12"/><local:StatisticsChart x:Name="RevenueChart" Height="290"/></StackPanel>
                                </Border>
                                <Border Grid.Column="2" Style="{StaticResource Card}">
                                    <StackPanel>
                                        <TextBlock Text="Состояние заказов" FontSize="18" FontWeight="SemiBold"/>
                                        <Grid Margin="0,22,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="Нужно выполнить" Foreground="{StaticResource MutedBrush}"/><TextBlock Grid.Column="1" x:Name="StatNew" FontSize="17" FontWeight="Bold"/></Grid>
                                        <Grid Margin="0,18,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="Активные проблемы" Foreground="{StaticResource MutedBrush}"/><TextBlock Grid.Column="1" x:Name="StatProblems" FontSize="17" FontWeight="Bold" Foreground="{StaticResource RedBrush}"/></Grid>
                                        <Grid Margin="0,18,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="Возвраты" Foreground="{StaticResource MutedBrush}"/><TextBlock Grid.Column="1" x:Name="StatReturns" FontSize="17" FontWeight="Bold" Foreground="{StaticResource AmberBrush}"/></Grid>
                                        <Grid Margin="0,18,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="Покупки" Foreground="{StaticResource MutedBrush}"/><TextBlock Grid.Column="1" x:Name="StatPurchases" FontSize="17" FontWeight="Bold"/></Grid>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>

                    <ScrollViewer x:Name="SettingsView" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                        <StackPanel MaxWidth="880" HorizontalAlignment="Left">
                            <Border Style="{StaticResource Card}">
                                <StackPanel>
                                    <TextBlock Text="Подключение" FontSize="19" FontWeight="SemiBold"/>
                                    <TextBlock Text="Тот же Pairing URL, что используется на Android. Токен хранится зашифрованным для текущего пользователя Windows." Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,5,0,14"/>
                                    <TextBox x:Name="PairingUrlBox"/>
                                    <WrapPanel Margin="0,12,0,0"><Button Style="{StaticResource AccentButton}" Content="Проверить и сохранить" Click="SaveConnectionButton_Click"/><Button Content="Тест уведомления" Margin="8,0,0,0" Click="TestNotificationButton_Click"/></WrapPanel>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource Card}" Margin="0,14,0,0">
                                <StackPanel>
                                    <TextBlock Text="Windows" FontSize="19" FontWeight="SemiBold"/>
                                    <CheckBox x:Name="MonitoringCheck" Content="Мониторинг включён" Margin="0,16,0,0"/>
                                    <CheckBox x:Name="NotificationsCheck" Content="Показывать системные уведомления" Margin="0,12,0,0"/>
                                    <CheckBox x:Name="AutoStartCheck" Content="Запускать вместе с Windows" Margin="0,12,0,0"/>
                                    <CheckBox x:Name="CloseToTrayCheck" Content="Сворачивать в область уведомлений при закрытии" Margin="0,12,0,0"/>
                                    <Button Content="Сохранить настройки Windows" HorizontalAlignment="Left" Margin="0,16,0,0" Click="SaveWindowsSettingsButton_Click"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource Card}" Margin="0,14,0,0">
                                <StackPanel>
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Автосообщения" FontSize="19" FontWeight="SemiBold"/><TextBlock Text="Общие для Android и Windows — сервер остаётся единственным отправителем." Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0"/></StackPanel><Button Grid.Column="1" Content="Загрузить" Padding="12,7" Click="LoadRepliesButton_Click"/></Grid>
                                    <CheckBox x:Name="DisableRepliesCheck" Content="Отключить все сообщения" Margin="0,18,0,0"/>
                                    <TextBlock Text="После оплаты" FontWeight="SemiBold" Margin="0,18,0,8"/>
                                    <StackPanel x:Name="MessagesPanel"/>
                                    <Button Content="＋ Добавить сообщение" HorizontalAlignment="Left" Margin="0,9,0,0" Click="AddMessageButton_Click"/>
                                    <TextBlock Text="После подтверждения мной выполнения" FontWeight="SemiBold" Margin="0,20,0,8"/>
                                    <TextBox x:Name="FulfillmentMessageBox" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="70"/>
                                    <TextBlock x:Name="FulfillmentHint" Foreground="#728096" FontSize="12" TextWrapping="Wrap" Margin="3,5,3,0"/>
                                    <Border Height="1" Background="#24FFFFFF" Margin="0,22,0,18"/>
                                    <CheckBox x:Name="SleepEnabledCheck" Content="Предупреждать покупателя, когда продавец может спать"/>
                                    <Grid Margin="0,13,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="10"/><ColumnDefinition/><ColumnDefinition Width="10"/><ColumnDefinition Width="1.5*"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Начало" Foreground="{StaticResource MutedBrush}" FontSize="12"/><ComboBox x:Name="SleepStartBox" IsEditable="True" Margin="0,5,0,0"/></StackPanel><StackPanel Grid.Column="2"><TextBlock Text="Конец" Foreground="{StaticResource MutedBrush}" FontSize="12"/><ComboBox x:Name="SleepEndBox" IsEditable="True" Margin="0,5,0,0"/></StackPanel><StackPanel Grid.Column="4"><TextBlock Text="Часовой пояс" Foreground="{StaticResource MutedBrush}" FontSize="12"/><ComboBox x:Name="SleepTimezoneBox" IsEditable="True" Margin="0,5,0,0"/></StackPanel></Grid>
                                    <TextBlock Text="Сообщение во время сна" FontWeight="SemiBold" Margin="0,18,0,8"/>
                                    <TextBox x:Name="SleepMessageBox" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="70"/>
                                    <TextBlock x:Name="SleepHint" Foreground="#728096" FontSize="12" TextWrapping="Wrap" Margin="3,5,3,0"/>
                                    <Button Style="{StaticResource AccentButton}" Content="Сохранить автосообщения" HorizontalAlignment="Left" Margin="0,20,0,0" Click="SaveRepliesButton_Click"/>
                                    <TextBlock x:Name="RepliesStatus" Foreground="{StaticResource MutedBrush}" Margin="0,10,0,0"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
