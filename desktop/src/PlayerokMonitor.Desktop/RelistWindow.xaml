<Window x:Class="PlayerokMonitor.Desktop.RelistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Выставить товар снова"
        Width="520"
        Height="580"
        MinHeight="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{StaticResource PageBrush}"
        ShowInTaskbar="False">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40" ResizeBorderThickness="7" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="{StaticResource PageBrush}">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="40"/><RowDefinition Height="*"/></Grid.RowDefinitions>
            <Border Background="{StaticResource ChromeBrush}" BorderBrush="{StaticResource DividerBrush}" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="46"/></Grid.ColumnDefinitions>
                    <TextBlock Text="Выставить снова" FontWeight="SemiBold" FontSize="12" VerticalAlignment="Center" Margin="14,0,0,0"/>
                    <Button Grid.Column="1" Style="{StaticResource CloseWindowButton}" shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="CloseButton_Click" ToolTip="Закрыть">
                        <Path Data="M0,0 L10,10 M10,0 L0,10" Width="10" Height="10" Stroke="{StaticResource TextBrush}" StrokeThickness="1" Stretch="Fill"/>
                    </Button>
                </Grid>
            </Border>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="20,18,20,22">
                    <TextBlock x:Name="ItemTitle" Text="Проверяю исходный товар…" FontSize="20" FontWeight="SemiBold" TextWrapping="Wrap"/>
                    <TextBlock Text="Сервер сохранит обложку и параметры. Один заказ можно использовать для повторной публикации только один раз." Foreground="{StaticResource MutedBrush}" FontSize="12" TextWrapping="Wrap" Margin="0,5,0,16"/>

                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Цена объявления" FontWeight="SemiBold" FontSize="13"/>
                            <TextBox x:Name="PriceBox" Margin="0,7,0,0"/>
                            <TextBlock x:Name="PriceHint" Foreground="{StaticResource MutedBrush}" FontSize="11" TextWrapping="Wrap" Margin="2,4,0,0"/>
                            <Border Height="1" Background="{StaticResource DividerBrush}" Margin="0,14"/>
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Premium-продвижение" FontWeight="SemiBold" FontSize="13"/>
                                    <TextBlock Text="Стоимость рассчитает Playerok для новой цены" Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,2,0,0"/>
                                </StackPanel>
                                <CheckBox Grid.Column="1" x:Name="PremiumCheck" VerticalAlignment="Center"/>
                            </Grid>
                            <Button x:Name="PreviewButton" Style="{StaticResource AccentButton}" Content="Рассчитать условия" HorizontalAlignment="Left" Margin="0,15,0,0" Click="PreviewButton_Click"/>
                        </StackPanel>
                    </Border>

                    <Border x:Name="OfferCard" Style="{StaticResource Card}" Visibility="Collapsed" Margin="0,10,0,0" Background="{StaticResource ElevatedBrush}">
                        <Grid>
                            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Условия публикации" FontSize="13" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" x:Name="OfferFee" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}"/>
                            </Grid>
                            <TextBlock Grid.Row="1" x:Name="OfferDetails" Foreground="{StaticResource MutedBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,8,0,0"/>
                            <Button Grid.Row="2" x:Name="PublishButton" Style="{StaticResource AccentButton}" Content="Подтвердить публикацию" HorizontalAlignment="Left" Margin="0,14,0,0" Click="PublishButton_Click"/>
                        </Grid>
                    </Border>
                    <TextBlock x:Name="StatusText" Foreground="{StaticResource MutedBrush}" FontSize="11" TextWrapping="Wrap" Margin="2,10,2,0"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
