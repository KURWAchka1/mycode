# Playerok Monitor Desktop 1.1.1

- Исправлено подключение к VPS: фактический ответ `/health` в формате `OK` теперь поддерживается наравне с JSON-ответом.
- Убрана техническая ошибка `invalid start of a value` при проверке Pairing URL.
- Неожиданный или отрицательный ответ health-check теперь отклоняется корректно, без ложного подключения.
- Добавлены регрессионные тесты для текстового `OK`, JSON `{ "ok": true }` и отрицательного ответа.

Версия устанавливается поверх 1.1.0 и сохраняет настройки. Pairing URL и серверный протокол менять не требуется.
